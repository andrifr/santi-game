node_modules/
.playwright-mcp/
*.log
.DS_Store
Thumbs.db

# Third-party music kept out of a public repo. Anything in here is
# someone else's recording; the game's own tunes are written in code
# (SG.audio.melody) and voice clips are recorded by the family.
assets/k3songs/

# Audio dropped into the project root. The repo is public and Pages
# serves it, so anything committed here is published - keep loose
# tracks out of `git add -A`.
/*.mp3
